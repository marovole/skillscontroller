# Skills Controller 官网

这是 Skills Controller 项目的官方网站，使用 Astro 构建的静态站点。

## 功能特性

- 📱 响应式设计，支持移动端和桌面端
- 🎨 Claude 官网风格设计（淡米白色 + 淡橙色）
- 🔍 技能分类过滤
- 📥 单个技能下载
- 📦 批量技能包打包下载
- ⚡ 静态生成，性能优异

## 项目结构

```
website/
├── src/
│   ├── components/       # Astro 组件
│   ├── layouts/          # 页面布局
│   ├── pages/            # 页面路由
│   ├── data/             # 技能数据
│   ├── utils/            # 工具函数
│   └── styles/           # 全局样式
├── public/               # 静态资源
└── dist/                 # 构建输出
```

## 开发命令

```bash
# 安装依赖
npm install

# 启动开发服务器 (http://localhost:4321)
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 技能数据管理

技能数据存储在 `src/data/skills.ts` 中。要添加新技能：

1. 在 `skills` 数组中添加新的技能对象
2. 确保包含所有必需字段（id, name, description, category, source, triggers, priority, content）
3. 重新构建网站

## 部署

### Cloudflare Pages

1. 连接 GitHub 仓库
2. 构建命令：`npm run build`
3. 输出目录：`dist`
4. 自动部署

### 其他静态托管

构建后 `dist/` 目录可以部署到任何静态托管服务：
- Vercel
- Netlify
- GitHub Pages
- AWS S3 + CloudFront

## 技术栈

- **框架**: Astro 4.0+
- **语言**: TypeScript
- **样式**: 原生 CSS（CSS 变量）
- **打包**: JSZip（客户端 ZIP 打包）
- **字体**: Google Fonts (Inter)

## 许可证

MIT
