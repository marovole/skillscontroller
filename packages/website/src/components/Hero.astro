---
---

<section class="hero">
  <div class="container">
    <div class="hero-content animate-fade-in">
      <h1 class="hero-title">
        Claude Code Skills Hub<br/>
        技能聚合与场景化打包平台
      </h1>
      <p class="hero-description">
        汇集 GitHub 高星开源项目的 240+ 精选技能，精心打造 21 个场景化技能包，
        覆盖开发、科研、办公等多个领域。让每一次 Claude Code 对话都拥有专家级能力。
      </p>
      <div class="hero-stats">
        <div class="stat">
          <span class="stat-number">240+</span>
          <span class="stat-label">技能</span>
        </div>
        <div class="stat">
          <span class="stat-number">16</span>
          <span class="stat-label">大领域</span>
        </div>
        <div class="stat">
          <span class="stat-number">21</span>
          <span class="stat-label">场景包</span>
        </div>
        <div class="stat">
          <span class="stat-number">9+</span>
          <span class="stat-label">开源项目</span>
        </div>
      </div>

      <!-- 浏览与下载入口 -->
      <div class="quick-action-section">
        <div class="quick-action-header">
          <h3 class="quick-action-title">立即开始</h3>
          <p class="quick-action-desc">浏览 21 个场景化技能包，选择最适合你的专家助手</p>
        </div>
        <div class="quick-action-actions">
          <a href="#all-packages" class="btn btn-primary">浏览技能包</a>
          <a href="#features" class="btn btn-secondary">了解核心功能</a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--spacing-2xl) var(--spacing-md);
    text-align: center;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin-bottom: var(--spacing-md);
    color: var(--color-text);
  }

  .hero-description {
    font-size: 1.125rem;
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto var(--spacing-lg);
    line-height: 1.7;
  }

  .hero-stats {
    display: flex;
    justify-content: center;
    gap: var(--spacing-xl);
    flex-wrap: wrap;
  }

  .stat {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--color-primary);
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    margin-top: var(--spacing-xs);
  }

  /* 快速操作区域样式 */
  .quick-action-section {
    margin-top: var(--spacing-xl);
    padding: var(--spacing-lg);
    background: linear-gradient(135deg, rgba(102, 126, 234, 0.08) 0%, rgba(118, 75, 162, 0.08) 100%);
    border: 1px solid rgba(102, 126, 234, 0.2);
    border-radius: var(--radius-lg);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .quick-action-header {
    margin-bottom: var(--spacing-md);
  }

  .quick-action-title {
    font-size: 1.5rem;
    font-weight: 600;
    margin: 0 0 var(--spacing-xs);
    color: var(--color-text);
  }

  .quick-action-desc {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    margin: 0;
  }

  .quick-action-actions {
    display: flex;
    justify-content: center;
    gap: var(--spacing-md);
  }

  .quick-action-actions .btn {
    padding: var(--spacing-sm) var(--spacing-lg);
    font-size: 0.9375rem;
  }

  @media (max-width: 768px) {
    .hero {
      padding: var(--spacing-xl) var(--spacing-sm);
    }

    .hero-stats {
      gap: var(--spacing-md);
    }

    .stat-number {
      font-size: 2rem;
    }

    .quick-action-section {
      margin-top: var(--spacing-lg);
      padding: var(--spacing-md);
    }

    .quick-action-title {
      font-size: 1.25rem;
    }

    .quick-action-actions {
      flex-direction: column;
      gap: var(--spacing-sm);
    }
  }
</style>
