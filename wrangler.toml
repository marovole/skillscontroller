# Cloudflare Pages 构建配置
name = "skillscontroller"
compatibility_date = "2024-01-01"

# Pages 构建设置（通过 Cloudflare Dashboard 配置）
# 构建命令: cd website && npm install && npm run build
# 构建输出目录: website/dist
# 根目录: /
# 环境变量: NODE_VERSION = 18
